<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Exports disabled
    |--------------------------------------------------------------------------
    |
    | Export formatters are automatically registered with the extension.
    | Sometimes you might wish to disable the user to see/use them,
    | add the formatters (their slug) to following array then
    |
    */

    'exports_disabled' => [

    ],

    /*
    |--------------------------------------------------------------------------
    | Imports disabled
    |--------------------------------------------------------------------------
    |
    | Import formatters are automatically registered with the extension.
    | Sometimes you might wish to disable the user to see/use them,
    | add the formatters (their slug) to following array then
    |
    */

    'imports_disabled' => [

    ],

];
